import styled from 'styled-components';
import { MaterialTreeRaw } from './MaterialTreeRaw';

export const MaterialTree = styled(MaterialTreeRaw)`
  display: grid;
  justify-items: left;
  padding: 3rem;
`;
